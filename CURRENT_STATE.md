# 斗地主当前工程状态

2026-09-11，Phase 1：独立 Guest Session 与朋友多人房间。

- 仓库：`lswger10/doudizhu`。施工分支：`codex/guest-rooms`。
- 基线：`codex/tidal-entertainment` 的 `6563665`，其中官端代码为 `4919a4c`；未从旧 main 重做。
- 本阶段代码及本地验证已完成；未推送、未合并、未部署。最后一轮测试见 [TEST_MATRIX.md](TEST_MATRIX.md)。
- 代码和证据随本地施工提交保存；复用原工作树，没有新建工作树。`codex/guest-rooms` 保留待审阅/发布，不合入旧 main。
- README 记录的 TEST 部署 `4919a4c` 是既有历史证据，本轮没有重新读取云端部署状态。
- 小家、Relay、Gateway、Mahjong 均未修改；没有模型付费验证或线上牌局操作。

## 当前行为

进入 `/doudizhu/`，服务端签发 HttpOnly Guest Cookie。输入昵称创建朋友房间，或者用房间码/邀请链接加入。
同名访客仍是不同身份；每个访客获得稳定席位。界面只呈现自己的手牌，刷新及短暂断线恢复原座。
“我的牌桌”从服务端当前身份的成员记录生成，关闭页面后重新进入也能返回已加入的牌桌。

朋友房间最多 3 位真人，开局前可加入，空位使用原有 Local AI。开局后不接管 AI；
离开只撤销该访客当前访问，保留席位，原访客可重新加入。断线/离开后的超时仍沿用裁判代打，
不是更换控制者。房主通过单独的“结束房间”操作停表并关闭房间，其他房间继续运行。
每位访客最多同时创建 8 个未结束房间。

原有 API/官端选人界面保留在单个 `classic` 专用房间，MCP 仍只接该裁判实例；
朋友房间不接 MCP，不泛化官端席位。新数据目录中首次创建 classic 的 Guest 是该房间唯一真人所有者，
其他 Guest 即使知道房间码也不能认领。该 Guest 可通过“我的牌桌”恢复，不重复创建。

## 状态归属及存档

| 状态 | 唯一权威来源 |
|---|---|
| Guest 身份与过期时间 | 数据目录 `guests.json`，仅保存 Cookie 的 SHA-256 摘要及随机 Guest UUID |
| 房主、房间类型、Guest→Seat、真人昵称、离开/结束标记 | 数据目录 `rooms.json` |
| 每个朋友房间的牌局、分数、资料、历史、头像 | `rooms/<房间码>/` 中独立的原有裁判存档 |
| 原有 classic 牌局和官端席位 | 原数据目录的裁判存档；保留既有 MCP 生命周期 |

`DoudizhuRooms` 负责授权与房间路由；`DoudizhuService` 仍独占裁判状态和计时器。
真人昵称在裁判资料中只是用于展示的投影；授权始终使用 Guest UUID 与席位绑定，不用昵称。
架构决定见 [DECISIONS.md](DECISIONS.md)，交接限制见 [PITFALLS.md](PITFALLS.md)。

## 发布前必须单独完成的事项

1. 旧数据目录只有 `state.json`、没有所有者绑定时，程序返回 `CLASSIC_OWNER_SETUP_REQUIRED`，
   不把旧牌桌送给第一位访客。发布准备新增管理员一次性请求 `classic-owner-claim.json`；操作见下节，线上尚未执行。
2. 小家现有入口仍受外层登录保护。本轮验证的是独立斗地主服务，未开放小家朋友入口，
   未验证跨公网邀请、反向代理 Cookie/Origin 或真实三星设备。
3. 权限/身份/邀请相关错误目前显示 message key，按用户要求尚未撰写最终产品文案。
4. Guest 有效期 30 天；同一浏览器 Cookie 保留时可恢复，清除 Cookie、到期或换设备不保证恢复。
   没有账号恢复或 Human↔AI 接管机制。
5. 单 Node 进程、原子 JSON 文件存储；不支持多个进程共同写同一数据目录。

没有数据库 migration、依赖变更、额外工作树、后台 worker 或消息总线。

## 旧主桌发布迁移（准备完成，尚未线上执行）

先备份整个数据卷目录，保留当前部署分支/提交。第一步发布 Guest 服务器，但继续保留小家反代登录保护。
用已确认的房主浏览器创建一间人工朋友核验房，记录 UI 显示的房间码。管理员核对该房间成员后，
仅在私有数据目录写入 `classic-owner-claim.json`，内容为 `{"sourceRoom":"<核验房间码>"}`。
文件不含 Cookie 或密钥；没有 HTTP 接口可写入它，不根据客户端 player_id 或昵称授予权限。

下一次启动在裁判/计时器启动之前验证：来源房间真实存在、其 owner 是有效的服务器 Guest、
旧主桌处于 lobby 且无官端席位。满足条件才把原 classic 绑定到该 owner，保留旧名字、分数和裁判数据。
成员表仍由 `rooms.json` 独占；请求成功后删除，崩溃后重复消费幂等，不重新选房主。
非法请求拒绝启动，保留请求和存档供管理员处理，不静默结束牌局。

核验房主能打开 classic、陌生 Guest 被拒绝后，再发布开放朋友入口的反代配置。
回滚先恢复外层登录保护，再切回原部署分支；新 `rooms/`、`guests.json`、`rooms.json` 需保留，
不能在已有新朋友牌局时用旧备份覆盖全部数据。只有确认无新写入才可恢复整个备份。
隧道使用原生 `--mcp.startup-wait-timeout 30s` 等待 Node 初始化，避免工具发现先于监听端口。
