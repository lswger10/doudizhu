# 斗地主工程决定

## 2026-09-11：最小多人房间边界

背景：原 HTTP/WS 将所有浏览器固定映射 `aurex`，只有单一裁判和存档。
这无法安全支持朋友各自入座。以 `codex/tidal-entertainment@6563665` 为基线，保留官端能力。

决定：增加一个 `DoudizhuRooms` 边界，使用标准库随机凭据与现有原子 JSON 写入。
Guest Cookie → 服务器 Guest UUID → 房间成员记录 → 稳定席位，之后才调用裁判。
HTTP、WS 首次连接、WS 动作及每次广播都执行该授权；不信任客户端 actor/player/seat 字段。
公开 Guest UUID 不是凭据，昵称不参与权限判断。每个朋友房间持有自己的原有裁判实例。

保留 `aurex/aevi/vex` 为内部席位键，避免改写规则与裁判；它们不再等于某个真人身份。
客户端将自己的 Seat 旋转显示在下方，旋转只改变显示位置，不改变发牌/出牌顺序。
成员表是权威来源，服务恢复时先配置 human kinds，再恢复计时器，避免真人恢复为 Local AI。

原 `classic` 专用牌桌保留现有 API/官端选择及单个 MCP 连接，朋友房间固定 Local AI 补空位。
本阶段不支持真人接管 AI，不将 MCP 扩展到多个房间或席位，不改官端工具协议及循环描述。

成员离开只标记 left，席位在房间生命期内保留。房主结束是独立命令：关闭房间、停止计时及后续写入，
不清理存档。既有裁判超时代打继续生效。前端停止执行/网络断开不等于房主结束。

发布边界：旧牌桌没有所有者证据，默认禁止自动认领；需要单独核准迁移。
不为跨系统登录引入小家核心改动，外层朋友可访问路径留给后续部署任务。

取舍：单进程、小规模文件存储，房间成员操作串行；不新增数据库、消息总线、通用 Controller、
Game Room UI、插件体系或 worker。只有实际吞吐需求出现后再评估拆分队列/存储。
会话到期、清 Cookie、跨设备恢复和账号体系不在本阶段；不以弱身份 fallback 绕过这些边界。

验证：[TEST_MATRIX.md](TEST_MATRIX.md) 的消融证明绑定边界提供独立价值，真实 HTTP/WS、浏览器及原官端回归共同验证。
