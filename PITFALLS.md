# 斗地主交接注意事项

- 不从旧 main 开发官端相关功能。Phase 1 基于 `codex/tidal-entertainment@6563665`，功能代码含 `4919a4c`。
- HTTP/WS 不得恢复固定 `aurex`，不得新增 `player_id`、昵称或 URL viewer 作为凭据。
  每次输出使用服务端绑定；原裁判广播参数是固定视角，服务器必须忽略它并逐连接重新生成快照。
- Guest Cookie 只保存浏览器凭据，服务端仅落摘要。不要把 Cookie、平台身份、lease、私牌写入工程文档/测试输出。
  Adapter 错误 detail 仅留私有裁判存档，不进入多人快照。
- `rooms.json` 是成员/昵称/房主/关闭状态的权威；游戏文件归各自 `rooms/<code>/`。
  不另存一份客户端席位权威，也不要让所有房间继续共用根目录 `state.json`。
- 重启前必须从成员表还原 human kinds，然后才允许裁判启动回合计时；否则真人可能被 Local AI 误操作。
- 结束房间不仅要清 timer，还要挡住已经排队的超时和 AI 回调写入；`suspended` 限制在对应裁判实例上。
  不能全局停止其他房间。保留原有规则里的超时代打，不把它叫作 Human↔AI 接管。
- 没有成员表的旧牌桌返回 `CLASSIC_OWNER_SETUP_REQUIRED`。不要删除状态文件来绕过认领保护。
  发布前须确认所有者、备份、迁移与回滚，并获得对进行中真实牌局中断的明确许可。
  管理员迁移请求操作见 CURRENT_STATE；必须在朋友入口开放前完成。请求拒绝时不要删除原 state.json。
- classic 仍只支持一个真人 owner；首次创建只适用于新数据目录。朋友不可通过邀请码领走 classic。
  已加入房间可在“我的牌桌”恢复；新建入口不意味着覆盖旧 classic。
- 邀请、身份、权限错误仅使用 message key；用户要求文案隔离，不能自行把这些 key 改成产品承诺。
- 原牌桌首位叫分者随机。测试应读取实际当前玩家，不假设第一手永远属于房主。
  绑定消融使用人工固定随机源并在 finally 恢复；HTTP/浏览器测试按真实回合推进。
- 朋友手牌在叫分阶段显示为静态卡图，出牌阶段才有可点击 `[data-card]`；不要用点击选择器错误判定叫分阶段未显示手牌。
- 前端保留旧手机横屏旋转布局。房间操作放原生折叠菜单，避免覆盖局数/倍率。真实三星设备仍待验收。
- 该服务器是单进程服务；原子文件锁不等于多进程状态一致性。不要用两个实例共写一个数据卷。
- 小家现有反代/登录不在本次施工中，独立服务测试通过不等于朋友已能穿过线上外层登录。
